map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    listen [::]:80;

    server_name arekha.duckdns.org www.arekha.duckdns.org;
    server_tokens off;
}
